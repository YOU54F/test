name: Package app

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:

  build:
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14, macos-12, ubuntu-20.04, windows-latest]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download prebuilds
        uses: actions/download-artifact@v3

      - name: Setup ruby
        uses: ruby/setup-ruby@master
        with:
            ruby-version: '3.2.5'
            
      - run: |
            sudo apt install -y gcc-10 g++-10
            sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-10 10
            sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-10 10
            sudo apt-get remove --purge --auto-remove cmake
            sudo apt-get update
            sudo apt-get install -y software-properties-common lsb-release curl
            sudo apt-get clean all
            curl https://apt.kitware.com/kitware-archive.sh | bash
            sudo apt-get install 
            sudo apt-get -y install sudo git curl build-essential pkg-config bison flex autoconf  \
            binutils-dev libevent-dev acl-dev libfmt-dev libjemalloc-dev libiberty-dev    \
            libdouble-conversion-dev liblz4-dev liblzma-dev libssl-dev libunwind-dev      \
            libboost-filesystem-dev libboost-program-options-dev libboost-system-dev      \
            libboost-iostreams-dev  libboost-date-time-dev libboost-context-dev           \
            libboost-regex-dev libboost-thread-dev libbrotli-dev libdwarf-dev libelf-dev  \
            libgoogle-glog-dev libffi-dev libgdbm-dev libyaml-dev libncurses-dev          \
            libreadline-dev libncurses-dev libreadline-dev ruby-dev ruby-bundler          \
            libutfcpp-dev
        if: runner.os == 'Linux'
        name: install ${{runner.os}} deps

      - if: runner.os == 'MacOS'
        name: install ${{runner.os}} deps
        run: |
            brew update
            brew install gnu-sed bash pkg-config bison flex binutils libffi gdbm zlib \
            ncurses double-conversion boost jemalloc fmt glog libevent libsodium lz4 xz \
            libyaml openssl@3
            export PATH="$(brew --prefix bison)/bin:$PATH"

      - if: runner.os == 'Windows'
        name: install ${{runner.os}} deps
        uses: msys2/setup-msys2@v2
        with:
          msystem: ucrt64
          path-type: minimal
          update: true
          install: >-
            git
            tar
            bison
            flex
          pacboy: >-
            cmake:p
            boost:p
            diffutils:p
            libevent:p
            double-conversion:p
            fmt:p
            glog:p
            dlfcn:p
            ninja:p
            gtest:p
            autotools:p
            ncurses:p
            ruby:p
            toolchain:p
            openssl:p
            make:p
            libyaml:p
            libxslt:p
      - run: gem install tebako
        name: install packager

      - run: tebako setup
        name: setup press


      - run: tebako press -p --root=app --entry-point=app --output=pact-cli-${{runner.OS}}-${{runner.arch}}
        name: press app